{
  "name": "guagua-carpool",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "vitest run",
    "check:json": "node -e \"const fs=require('fs'); for (const f of ['project.config.json','miniprogram/app.json','miniprogram/sitemap.json','database/indexes.json']) { if (!fs.existsSync(f)) continue; JSON.parse(fs.readFileSync(f,'utf8')); } console.log('json ok')\""
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
